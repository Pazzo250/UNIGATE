This folder is an archive marker for the `server_ts` TypeScript server project.

The original source remains in `server_ts/`. To fully remove the original,
delete `server_ts/` and rename this folder to `server_ts`.

Created by automated maintenance on behalf of the repository owner.
