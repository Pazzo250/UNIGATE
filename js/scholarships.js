// scholarships.js - placeholder for scholarships page behavior
function searchScholarships() {
  const q = (document.getElementById('scholarshipSearch') || {}).value || '';
  alert('Search is a demo. You searched: ' + q);
}

function applyForScholarship(id) {
  alert('Apply flow not implemented in demo. Scholarship: ' + id);
}
