# Archived

The `server_ts` TypeScript server has been archived. This folder remains for
reference but is no longer the active server runtime. Consider deleting or
removing this folder from version control if you no longer need it.

Archived on 2026-02-21 by automated maintenance.
